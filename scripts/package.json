{
  "name": "@xstreamroll/scripts",
  "private": true,
  "description": "Repo-level utility scripts shipped with CI (validator, lockfile check). The package has no published artifacts — its only purpose is to host its tests under scripts/__tests__/.",
  "scripts": {
    "test": "jest"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "testMatch": [
      "<rootDir>/__tests__/**/*.test.ts"
    ]
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^26.4.0",
    "jest": "^30.5.0",
    "ts-jest": "^29.4.12",
    "typescript": "^7.0.2"
  }
}
