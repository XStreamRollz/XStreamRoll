{
  "name": "@xstreamroll/types",
  "version": "1.0.0",
  "description": "Shared domain types for the XStreamRoll API, SDK, and web app",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "files": [
    "src"
  ],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/**/*.ts"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  }
}
